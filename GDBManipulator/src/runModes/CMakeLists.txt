set(dir src/runModes/)

set(src_runMode
        ${dir}AbstractRunMode.h
        ${dir}FlashMode.cpp
        ${dir}TestMode.h
        ${dir}FlashMode.h
        ${dir}TestMode.cpp
        ${dir}RunMode.cpp
        ${dir}RunMode.h
        PARENT_SCOPE )